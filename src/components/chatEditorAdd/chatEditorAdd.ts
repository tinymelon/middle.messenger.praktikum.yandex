import Block from "../../core/Block";

export class ChatEditorAdd extends Block {
    constructor(props: any) {
        super({
            ...props
        });
    }

    protected render(): string {
        return (`
            
        `);
    }
}
