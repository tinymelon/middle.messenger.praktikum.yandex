{
  "name": "messenger",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "tsc && vite build --emptyOutDir && node server",
    "build": "tsc && vite build --emptyOutDir"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "handlebars": "^4.7.8",
    "less": "^4.2.0",
    "typescript": "^5.2.2",
    "vite": "^4.4.5"
  }
}
