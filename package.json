{
  "name": "messenger",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite build --emptyOutDir && node server",
    "build": "vite build --emptyOutDir"
  },
  "engines": {
    "node": ">=18.17"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "less": "^4.2.0",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "vite-plugin-handlebars": "^1.6.0"
  }
}
